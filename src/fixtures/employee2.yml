entity: employee
connectedFields: ['profession', 'hrManager']
items:
  employee{3..5}:
    firstName: '{{name.firstName}}'
    lastName: '{{name.lastName}}'
    email: '{{internet.email}}'
    birthDate: '{{date.past}}'
    profession: '@profession1'
    hrManager: '@employee1'
    imageUrl: '{{image.avatar}}'
  employee{6..8}:
    firstName: '{{name.firstName}}'
    lastName: '{{name.lastName}}'
    email: '{{internet.email}}'
    birthDate: '{{date.past}}'
    profession: '@profession2'
    hrManager: '@employee2'
    imageUrl: '{{image.avatar}}'
  employee{12..14}:
    firstName: '{{name.firstName}}'
    lastName: '{{name.lastName}}'
    email: '{{internet.email}}'
    birthDate: '{{date.past}}'
    profession: '@profession2'
    hrManager: '@employee9'
    imageUrl: '{{image.avatar}}'
  employee{15..17}:
    firstName: '{{name.firstName}}'
    lastName: '{{name.lastName}}'
    email: '{{internet.email}}'
    birthDate: '{{date.past}}'
    profession: '@profession2'
    hrManager: '@employee10'
    imageUrl: '{{image.avatar}}'
  employee{18..20}:
    firstName: '{{name.firstName}}'
    lastName: '{{name.lastName}}'
    email: '{{internet.email}}'
    birthDate: '{{date.past}}'
    profession: '@profession2'
    hrManager: '@employee11'
    imageUrl: '{{image.avatar}}'
